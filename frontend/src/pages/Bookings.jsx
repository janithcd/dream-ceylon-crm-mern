const Bookings = () => {
    return (
        <div>
            <h2 className="fw-bold mb-1">Bookings</h2>
            <p className="text-muted">Booking management page coming next.</p>
        </div>
    );
};

export default Bookings;